<UserControl x:Class="Timelapse.Controls.ThumbnailGridVirtualized"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Focusable="False"
             PreviewMouseLeftButtonDown="OnMouseLeftButtonDown"
             PreviewMouseLeftButtonUp="OnMouseLeftButtonUp"
             MouseLeftButtonUp="OnMouseLeftButtonUp"
             MouseMove="OnMouseMove"
             LostMouseCapture="OnLostMouseCapture">
    <Grid>
        <ScrollViewer Name="ScrollViewer"
                      VerticalScrollBarVisibility="Visible"
                      HorizontalScrollBarVisibility="Disabled"
                      Background="Black"
                      ScrollChanged="ScrollViewer_ScrollChanged"
                      PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
            <Canvas Name="VirtualCanvas" Background="Black" HorizontalAlignment="Left" VerticalAlignment="Top" />
        </ScrollViewer>
        <!-- Home thumbnail preview shown during the deactivation animation -->
        <Border Name="HomePreviewBorder"
                Visibility="Collapsed"
                IsHitTestVisible="False"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="40,40,0,0"
                RenderTransformOrigin="0,0">
            <Border.RenderTransform>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Border.RenderTransform>
            <Image Name="HomePreviewImage" Stretch="Uniform"/>
        </Border>
        <Border Name="SelectionBadge"
                Background="#CC1C1C1C"
                CornerRadius="4"
                Padding="8,4"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Visibility="Collapsed"
                IsHitTestVisible="False">
            <TextBlock Name="SelectionBadgeText"
                       Foreground="White"
                       FontSize="12"
                       FontWeight="SemiBold"/>
        </Border>
    </Grid>
</UserControl>
