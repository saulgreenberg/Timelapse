<Window x:Class="Timelapse.Dialog.ExceptionShutdownDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:toolkit="clr-namespace:TimelapseWpf.Toolkit;assembly=TimelapseWpf.Toolkit"
        mc:Ignorable="d"
              Title="Timelapse needs to close." Width="650" MinWidth="650" Height="550" Loaded="Window_Loaded">
    <Grid  VerticalAlignment="Top">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <toolkit:FormattedMessageContent x:Name="Message" Grid.Row="0" Icon="{x:Static toolkit:DialogIconType.Error}"
                            DialogTitle ="Timelapse needs to close. Please report this error"
                            Problem="Timelapse encountered a problem, likely due to a bug. Please help us fix it! 
                                    [ni] Press [e]Mail this report[/e] to mail the bug report to Saul, the Timelapse developer.  
                                    [li 2] or, select [e]Copy report[/e] and paste the report into an email to [link:{x:Static constant:ExternalLinks.EmailAddressAsMailTo}|{x:Static constant:ExternalLinks.EmailAddress}].
                                    [ni] Describe what you were doing at the time of the crash."
                            Result = "Timelapse will shut down."
                            Hint="[ni] [e]Restart Timelapse[/e], as this usually picks up where you left off. 
                                  [ni] If problems recur or are serious, zip up your [i].tdb[/i] and [i].ddb[/i] files and email them to [link:{x:Static constant:ExternalLinks.GmailAddressAsMailTo}|{x:Static constant:ExternalLinks.GmailAddress}] to help him figure out what happened (his other [e]ucalgary.ca[/e] address does not accept zip files).
                                  [ni] If your data file was damaged (rare), you may be able to restore as saved version from the [i]Backup[/i] folder."/>
        <StackPanel Orientation="Horizontal"  Grid.Row="1">
            <TextBlock Text="Technical problem description" Margin="10,10,5,10" FontSize="14" FontWeight="Bold"/>
            <TextBlock Text="to send to the Timelapse Developer" Margin="0,10,20,10" FontSize="14" FontStyle="Italic"/>
        </StackPanel>
        <ScrollViewer HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Auto"  Grid.Row="2" Height="200" Margin="20,10,20,10">
            <TextBox Name="ExceptionReport" VerticalAlignment="Top" FontSize="10" TextWrapping="Wrap" IsReadOnly="True" />
        </ScrollViewer>
        <Grid Grid.Row="3" Margin="0,0,10,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
        <!--<StackPanel  Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="3" Margin="0,0,10,0">-->
            <Button Grid.Column="0" Name="CopyReport" IsCancel="false" VerticalAlignment="Bottom" HorizontalAlignment="Left" Padding="10" Margin="15,10,10,10" Click="CopyButton_Click">Copy report</Button>
            
            <Button Grid.Column="2" Name="CancelButton" IsCancel="True" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Padding="10" Margin="10,10,10,10" Click="CancelButton_Click">Exit</Button>
            <Button Grid.Column="3" Name="MailButton" IsDefault="false" AutomationProperties.AutomationId="MailButton" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Padding="10" Margin="10,10,10,10" Click="MailButton_Click">Mail this report...</Button>
        <!--</StackPanel>-->
        </Grid>

    </Grid>
</Window>
