<Window x:Class="Timelapse.Dialog.ExceptionShutdownDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:toolkit="clr-namespace:TimelapseWpf.Toolkit;assembly=TimelapseWpf.Toolkit"
        mc:Ignorable="d"
              Title="Timelapse needs to close." Width="650" MinWidth="650" Height="650" Loaded="Window_Loaded">
    <Grid  >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <toolkit:FormattedMessageContent x:Name="Message" Grid.Row="0" Icon="{x:Static toolkit:DialogIconType.Error}"/>
        <StackPanel Orientation="Horizontal"  Grid.Row="1">
            <TextBlock Text="Technical problem description" Margin="10,10,5,10" FontSize="14" FontWeight="Bold"/>
            <TextBlock Text="to send to the Timelapse Developer" Margin="0,10,20,10" FontSize="14" FontStyle="Italic"/>
        </StackPanel>
        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto"  Grid.Row="2" Margin="20,10,20,10">
            <TextBox Name="ExceptionReport" VerticalAlignment="Top" FontSize="10" TextWrapping="Wrap" IsReadOnly="True" />
        </ScrollViewer>
        <Grid Grid.Row="3" Margin="0,0,10,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
        <!--<StackPanel  Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="3" Margin="0,0,10,0">-->
            <Button Grid.Column="0" Name="MailButton" IsDefault="false" AutomationProperties.AutomationId="MailButton" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Padding="10" Margin="10,10,10,10" Click="MailButton_Click">Mail this report...</Button>
            <Button Grid.Column="1" Name="CopyReport" IsCancel="false" VerticalAlignment="Bottom" HorizontalAlignment="Left" Padding="10" Margin="15,10,10,10" Click="CopyButton_Click">Copy report</Button>
            <Button Grid.Column="3" Name="ContinueButton" IsCancel="false" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Padding="10" Margin="10,10,10,10" Click="ContinueButton_Click">Continue anyways</Button>
            <Button Grid.Column="4" Name="ExitButton" IsCancel="True" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Padding="10" Margin="10,10,10,10" Click="ExitButton_Click">Exit Timelapse</Button>
        <!--</StackPanel>-->
        </Grid>

    </Grid>
</Window>
