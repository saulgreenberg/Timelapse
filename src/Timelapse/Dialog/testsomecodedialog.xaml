<Window  x:Class="Timelapse.Dialog.TestSomeCodeDialog"
                                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                                xmlns:toolkit="http://timelapsewpf.toolkit/xaml"
                                mc:Ignorable="d"
                                Title="Test for SpeciesNet" Height="900" Width="740" Loaded="TestSomeCodeDialog_OnLoaded">
    <Grid>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header/Instructions -->
            <toolkit:FormattedMessageContent x:Name="TestMessage"  Grid.Row="0"
                            Icon="{x:Static toolkit:DialogIconType.Question}"
                            DialogTitle="Test for species net"
                            What="Select one of these"
                            ShowExplanationVisibility="True" />

        </Grid>

    </Grid>
</Window  >
