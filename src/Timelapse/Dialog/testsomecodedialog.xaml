<Window  x:Class="Timelapse.Dialog.TestSomeCodeDialog"
                                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                                xmlns:toolkit="http://timelapsewpf.toolkit/xaml"
                                mc:Ignorable="d"
                                Title="Export data into image files as XMP/IPTC metadata" Height="900" Width="740" Loaded="TestSomeCodeDialog_OnLoaded">
    <Grid>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header/Instructions -->
            <toolkit:FormattedMessageContent x:Name="TestMessage"  Grid.Row="0"
                            Icon="{x:Static toolkit:DialogIconType.Question}"
                            DialogTitle="Test"
                            What="Test dialog."
                            ShowExplanationVisibility="True" />
            <ComboBox Name="CB" Width="200" Height="40" Grid.Row="1"/>
            <ComboBox Name="CBX" Width="200" Height="40" Grid.Row="2"/>
        </Grid>

    </Grid>
</Window  >
