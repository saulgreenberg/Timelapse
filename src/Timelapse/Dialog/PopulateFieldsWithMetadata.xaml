<controls:BusyableDialogWindow 
    x:Class="Timelapse.Dialog.PopulateFieldsWithMetadata"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="clr-namespace:Timelapse.Controls"
    xmlns:toolkit="clr-namespace:TimelapseWpf.Toolkit;assembly=TimelapseWpf.Toolkit"
    mc:Ignorable="d"
    Title="Populate a data field with metadata" 
	MinHeight="600" MinWidth="768"
	Height="856" Width="840"
	ResizeMode="CanResizeWithGrip"
	ShowInTaskbar="False" 
    WindowStartupLocation="CenterOwner" 
    Loaded="Window_Loaded" Closing="Window_Closing">

    <Grid>
        <DockPanel Grid.Row="0">

            <toolkit:FormattedMessageContent x:Name="PopulateAllMessage" DockPanel.Dock="Top"
                            Icon="{x:Static toolkit:DialogIconType.Question}"
                            DialogTitle="Populate Data Fields with Selected Metadata"
                            What="Images often embed metadata, some of which may be useful to capture as data.[br]You can direct Timelapse on how to populate data fields with metadata."
                            Solution="Match the desired metadata to a data field to populate those data fields for all your currently selected files.
[ni] Inspect the current file's metadata (and its example value) to see if your files contain metadata of interest
[ni] For each metadata of interest, use the drop-down menu to select which data field should be populated with the metadata's value.                                          
[ni] Click the [e]Populate[/e] button to apply your metadata selections to all your currently selected files."
                            Hint="[li]Try both inspection tools: [e]MetadataExtractor[/e] is faster. [e]ExifTool[/e] is better at finding all metadata fields but much slower.
                                  [li] Only [e]Note[/e] or [e]Multiline[/e] fields can be populated
                                  [li] Be patient! It does take time to do this (especially with [e]ExifTool[/e]) if you have a large number of files."
                            Result="Each file's data fields will be populated with each file's metadata values for the metadata fields selected."
                            ShowExplanationVisibility="True" />

            <toolkit:FormattedMessageContent x:Name="DatesOnlyMessage" DockPanel.Dock="Top" Visibility="Collapsed"
                            Icon="{x:Static toolkit:DialogIconType.Question}"
                            DialogTitle="Populate Date/Time fields from a metadata field..."
                            What="Metadata can contain various date/time information, e.g., creation data vs. last modified date, etc.
                                  [li]If those are of interest, you can populate any [e]Date/Time[/e] data field with the file's date/time metadata.     
                                  [li]Some files, especially videos, record its creation date/time in a metadata field unknown to Timelapse.
                                  [li]This can also mitigate issues if the automatically filled-in [e]DateTime[/e] field appears wrong: if that file contains a metadata field with the correct date, you can use that instead."
                            Solution="Match the desired metadata to the [e]Date/Time[/e] data field to populate that field for all your currently selected files.    
[ni] The list below includes metadata found by either [e]MetadataExtractor[/e] or [e]ExifTool[/e]. Only  metadata fields that can be interpretted as containing a date and/or time are listed.
[ni] Scan each  metadata row to see if it contains a correct Date/Time for the example file.
[ni] If one exists, use the drop-down menu in the [e]Data Field[/e] column to select the DateTime data field.
[ni] Click the [e]Populate[/e] button to update the [e]DateTime[/e] field(s) for your currently selected files with the metadata value(s) found in each file."
                             Hint="[ni]Try both inspection tools: [e]MetadataExtractor[/e] is faster. [e]ExifTool[/e] is better at finding all metadata fields but much slower.
                                   [ni]Be patient! It does take time to do this (especially with [e]ExifTool[/e]) if you have a large number of files."
                            Result="Each file's selected [e]Date/Time[/e] field will be populated with each file's metadata value for the metadata fields selected.
                               [br]Files missing that metadata field, or that contain an invalid date will remain unchanged."
                            ShowExplanationVisibility="True" />
            
            <DockPanel LastChildFill="True" >
                <StackPanel Orientation="Vertical" DockPanel.Dock="Top" VerticalAlignment="Stretch" Margin="10,0,10,0">
                    <Rectangle Fill="#FF868686" Height="2" />
                </StackPanel>
                <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right">
                    <CheckBox x:Name="ClearIfNoMetadata"  VerticalAlignment="Center" HorizontalAlignment="Left" Padding="5,0,50,0" Margin="10,0,0,0" Checked="ClearIfNoMetadata_Checked">Clear the data field if the image file cannot be read or if it is missing that metadata field</CheckBox>
                    <Button x:Name="CancelButton" IsCancel="True" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Padding="10" Margin="10,10,10,10" Click="CancelButton_Click">_Cancel</Button>
                    <Button x:Name="StartDoneButton" IsDefault="True" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Padding="10" Margin="10,10,10,10"  IsEnabled="False" Click="Start_Click">_Populate</Button>
                </StackPanel>

                <controls:FileMetadataGrid x:Name="MetadataGrid"  DockPanel.Dock="Top" VerticalAlignment="Stretch"/>

                <Grid Name="FeedbackPanel" Visibility="Collapsed" DockPanel.Dock="Top">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Name="PopulatingMessage"  Text="--" 
                               Grid.Column="0" Grid.Row="0" 
                               FontWeight="Bold"
                               HorizontalAlignment="Left" Margin="10,10,10,0" />
                    <DataGrid Name="FeedbackGrid" 
                        Grid.Column="0" Grid.Row="1"
                        Margin="0,0,10,0" ColumnWidth="*" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                        SelectionUnit="FullRow" SelectionMode="Single" 
                        AutoGenerateColumns="True" AutoGeneratedColumns="FeedbackDatagrid_AutoGeneratedColumns" />
                </Grid>
            </DockPanel>
        </DockPanel>
        <controls:BusyCancelIndicator x:Name="BusyCancelIndicator" IsBusy="False" Grid.Row="0" CancelClick="CancelAsyncOperationButton_Click"/>
    </Grid>
</controls:BusyableDialogWindow>
