<?xml version="1.0" encoding="utf-8"?>
<dialog:FileMetadataPopulateBase
    x:Class="Timelapse.Dialog.FileMetadataPopulateDatesOnly"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="clr-namespace:Timelapse.Controls"
    xmlns:dialog="clr-namespace:Timelapse.Dialog"
    xmlns:toolkit="clr-namespace:TimelapseWpf.Toolkit;assembly=TimelapseWpf.Toolkit"
    mc:Ignorable="d"
    Title="Populate dates and times from a metadata field"
    MinHeight="600" MinWidth="768"
    Height="856" Width="840"
    ResizeMode="CanResizeWithGrip"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterOwner"
    Loaded="Window_Loaded" Closing="Window_Closing">

    <Grid>
        <DockPanel Grid.Row="0">

            <toolkit:FormattedMessageContent x:Name="DatesOnlyMessage" DockPanel.Dock="Top"
                            Icon="{x:Static toolkit:DialogIconType.Question}"
                            DialogTitle="Populate Date/Time fields from a metadata field..."
                            What="Metadata can contain various date/time information, e.g., creation data vs. last modified date, etc.
                                  [li]You can populate any [e]DateTime, Date, or Time[/e] data field with the file's date/time metadata.
                                  [li]This can correct issues (especially with videos) if the automatically filled-in [e]DateTime[/e] field appears wrong, where you can replace it with the date found in a metadata field.
                                  [li]If the metadata includes previously exported Timelapse data (in an [e]XMP-TimelapseData Metadata group[/e]), you can import that as well."
                            Solution="Match the desired metadata to the [e]Date/Time[/e] data field to populate that field for all your currently selected files.
[ni] The list below includes what appears to be [i]Date[/i] and/or [i]Time[/i] metadata found by [e]ExifTool[/e] or [e]MetadataExtractor[/e].
[ni] Scan each  metadata row to find one (if any) that contains the correct [i]Date[/i] and/or [i]Time[/i] for the example file.
[ni] Select the desired [e]DateTime[/e] data field entry from the [e]Data field[/e] dropdown menu that you want populated. 
[ni] Click [e]Populate[/e] to update the [e]DateTime[/e] field(s) for your currently selected files with the metadata value(s) found in each file."
                            Hint="[ni]Try both inspection tools: [e]ExifTool[/e] is much better at finding all metadata fields, but [e]MetadataExtractor[/e] is faster at populating large number of files.
                                             [ni][e]XMP-TimelapseData[/e], if present, contains data previously exported from Timelapse. Clicking [e]Select matching data fields[/e] will try to match those entries with exiting data fields."
                            Result="Each file's selected [e]Date/Time[/e] field(s) will be populated with each file's metadata value for the selected metadata fields.
                               [br]Files missing that metadata field, or that contain an invalid date/time will remain unchanged."
                            ShowExplanationVisibility="True" />

            <DockPanel LastChildFill="True" >
                <StackPanel Orientation="Vertical" DockPanel.Dock="Top" VerticalAlignment="Stretch" Margin="10,0,10,0">
                    <Rectangle Fill="#FF868686" Height="2" />
                </StackPanel>
                <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="BackButton" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Padding="10" Margin="10,10,10,10" Visibility="Collapsed" Click="Back_Click">_Back</Button>
                    <Button x:Name="CancelButton" IsCancel="True" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Padding="10" Margin="10,10,10,10" Click="CancelButton_Click">_Cancel</Button>
                    <Button x:Name="PreviewButton" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Padding="10" Margin="10,10,10,10" IsEnabled="False" Click="Preview_Click">P_review</Button>
                    <Button x:Name="StartDoneButton" IsDefault="True" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Padding="10" Margin="10,10,10,10"  IsEnabled="False" Click="Start_Click">_Populate</Button>
                </StackPanel>

                <controls:FileMetadataGrid x:Name="MetadataGrid"  DockPanel.Dock="Top" VerticalAlignment="Stretch"/>
                <Grid Name="FeedbackPanel" Visibility="Collapsed" DockPanel.Dock="Top">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Column="0" Grid.Row="0" Orientation="Horizontal" Margin="10,10,10,0">
                        <TextBlock Name="PopulatingMessage"  Text="--"
                                   FontWeight="Bold"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Left" />
                        <RadioButton Name="OnlyShowIssuesRadioButton"
                                     Content="Only show issues"
                                     GroupName="FeedbackDisplayGroup"
                                     IsChecked="True"
                                     VerticalAlignment="Center"
                                     Margin="20,0,0,0"
                                     Checked="FeedbackDisplayRadioButton_Changed"/>
                        <RadioButton Name="ShowEverythingRadioButton"
                                     Content="Show everything"
                                     GroupName="FeedbackDisplayGroup"
                                     IsChecked="False"
                                     VerticalAlignment="Center"
                                     Margin="10,0,0,0"
                                     Checked="FeedbackDisplayRadioButton_Changed"/>
                    </StackPanel>
                    <DataGrid Name="FeedbackGrid"
                        Grid.Column="0" Grid.Row="1"
                        Margin="0,0,10,0" ColumnWidth="*" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                        SelectionUnit="FullRow" SelectionMode="Single"
                        AutoGenerateColumns="True" AutoGeneratedColumns="FeedbackDatagrid_AutoGeneratedColumns" />
                </Grid>
            </DockPanel>
        </DockPanel>
        <controls:BusyCancelIndicator x:Name="BusyCancelIndicator" IsBusy="False" Grid.Row="0" CancelClick="CancelAsyncOperationButton_Click"/>
    </Grid>
</dialog:FileMetadataPopulateBase>
