<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:toolkit="http://timelapsewpf.toolkit/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="CommonResources.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!--#region ImprintNoteTextBox Style: Properties for ImprintAutoCompleteTextBox-->
    <Style x:Key="ImprintNoteTextBox" TargetType="toolkit:ImprintAutoCompleteTextBox">
        <!-- Layout properties only - colors and focus behavior come from toolkit -->
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Padding" Value="1,0,1,0"/>
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Height" Value="25" />
        <Setter Property="Focusable" Value="True" />
    </Style>
    <!--#endregion-->

    <!--#region MultiLineTextBox Style -->
    <!--The top margin is a compromise as text shifts a bit depending on whether 1 or 2+ lines are showing -->
    <Style x:Key="MultiLineTextBox" TargetType="{x:Type toolkit:MultiLineText}">
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Padding" Value="1,3,1,0"/>
        <Setter Property="BorderThickness" Value="{StaticResource BorderThicknessCommonNormal}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrushCommonNormal}"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="VerticalContentAlignment" Value="Top"/>
        <Setter Property="Height" Value="25" />
        <Setter Property="Focusable" Value="True" />
        <Setter Property="IsReadOnly" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
    </Style>
    <!-- #endregion-->

</ResourceDictionary>
